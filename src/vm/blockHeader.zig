pub const BlockHeader = struct {
    // parent_hash: [32]u8,
    // uncles_hash: [32]u8,
    // coinbase: [20]u8,
    // state_root: [32]u8,
    // transaction_root: [32]u8,
    // receipt_root: [32]u8,
    // difficulty: u256,
    block_number: u64,
    // gas_limit: u64,
    // gas_used: u64,
    // timestamp: u256,
    // extra_data: []const u8,
    // mix_hash: [32]u8,
    // nonce: []const u8,

    // pub const BLOOM_BITS: u32 = 3;
    // pub const BLOOM_SIZE: usize = 256;
};
